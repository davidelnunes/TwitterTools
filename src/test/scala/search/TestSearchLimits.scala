package search

import org.scalatest.{FlatSpec, Matchers}

/**
 * Created by @davidelnunes on 09-02-2015.
 */
class TestSearchLimits extends FlatSpec with Matchers{
/*
  "Seach Tools" should "be able to retrieve more than 45000 tweets" in {
    val console = new TTConsole

    val toolConfig: ToolConfig = ToolConfig("twitter-tools")
    val search: SearchTools = SearchTools(toolConfig,Some(console))

    val keyword = "hello"

    search.tweetsFromKeyword(keyword,45001)

    //mtw.tweets(keyword).size should be (1000)
  }
*/
}
